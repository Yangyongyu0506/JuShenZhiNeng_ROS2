# 好消息

跑通了第三、四、五讲

# 坏消息

关于小车一个轮子转不了（调换接口这个轮子能转但是另一个轮子转不了）的问题，换了块板子但是i2c连接没有7a口，过了一会儿啥都没了 :(

或许得多换几块板子和杜邦线……

谁去解决一下捏

# tips

- 给的例程里面硬编码了 `cap = VideoCapture(0)` ，这在好几个文件都是这样，所以我们必须确保摄像头的配置文件路径和名字是`/dev/video0`，如果不是可以拔掉摄像头连香橙派的USB-A线然后重新插一下

- 由于tightVNC连接不太稳定并且有点风险（），还有一种在mobaxterm里面可以让电脑显示摄像头实时图像的方法（这种方法建立在ssh上比较安全但是有延迟）：

    - 确保左上角的x server图标是彩色的，如果不是，单击
    - 设置mobaxterm的大图标setting—x11—display offset为1
    - 大图标session—ssh输入香橙派ip地址，在adva![image-20251011193438074](./assets/image-20251011193438074.png)nced ssh setting里面勾选x11 forwarding（默认勾选)
    - 在mobaxterm建立ssh的终端输入`echo $DISPLAY`理论上会显示localhost： blah blah
    - 然后在**mobexterm**里面python3运行程序就会将图像转发到电脑上了（我今天是这么干的）

    