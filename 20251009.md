整个项目在`/root/thuei-1`，不能换路径。。。

需要`conda activate masterpi-2`进入

# 第三讲

`/root/thuei-1/sdk-python/Function/ColorDetect.py`和`/root/thuei-1/sdk-python/Function/Avoidance.py`目前存在如下报错（也是唯一的错误）

```bash
(masterpi-2) root@orangepiaipro:~/thuei-1# python3 ./sdk-python/Functions/ColorDetect.py 
ColorDetect Init 
ColorDetect Start 
[ WARN:0@1.109] global cap_v4l.cpp:914 open VIDEOIO(V4L2:/dev/video0): can't open camera by index 
[ERROR:0@1.110] global obsensor_uvc_stream_channel.cpp:163 getStreamChannelGroup Camera index out of range
```

以下是排查

```bash
(masterpi-2) root@orangepiaipro:~/thuei-1# ls -l /dev/video* 
lrwxrwxrwx 1 root root 11 Oct 9 16:50 /dev/video0 -> /dev/video2 
crw-rw----+ 1 root video 81, 1 Oct 9 17:03 /dev/video1

(masterpi-2) root@orangepiaipro:~/thuei-1# v4l2-ctl --device=/dev/video2 --list-formats
ioctl: VIDIOC_ENUM_FMT 
    Type: Video Capture
```

问题应该出在`ColorDetect.py`main函数的`cap = blah blah`这一行硬编码摄像头索引是0，py找不到摄像头，或者读不到摄像头数据

**这个问题需要解决！**

# 第四讲

目前完成了小车电机和实际轮子的映射确认，映射关系如下（小车有灯的应该是前吧？）

电机1正转——右后轮正转

电机2正转——左后轮正转

电机3正转——右前轮反转

电机4正转——左前轮正转

以上映射关系可以在我写的`/root/thuei-1/MecanumControl/test.py`可以验证

对于教程的`/root/thuei/MecanumControl/Car_Forward_Demo.py`的chassis对象，映射关系如下

左前对应`1`，方向`-1`；右前对应`2`，方向`1`；左后未知；右后对应`4`，方向`-1`；

但是，最严重的问题是我们的板子似乎有问题！对于demo文件，左后轮跑不了，并且在随意互换两个轮子在板子上的接口后，左后轮可以跑但是被换的那个轮子跑不了！！！所以，我强烈怀疑是板子的接口有问题（应该是最靠近开关的那个）

**这个问题需要解决！**